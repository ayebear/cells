#ifndef WIDGETMAP_H
#define WIDGETMAP_H
/*
#include <map>
#include <functional>
#include "widget.h"

namespace gui
{

class WidgetMap
{
    using std::unique_ptr<Widget> WidgetPtr;

    public:
        WidgetMap();
        void add(const std::string& widgetType, const std::string name);

    private:
        std::map<std::string, WidgetPtr> widgets;
};

}
*/
#endif
